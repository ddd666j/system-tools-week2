# 实例 9：使用 timeit 比较列表与集合查询

## 原理

列表成员查询需要顺序扫描，平均时间复杂度为 O(n)；集合基于哈希表，平均为 O(1)。`timeit.repeat` 能在相同数据和函数边界下重复计时，使用中位数可降低单次系统抖动的影响。

## 步骤

1. 使用固定随机种子生成 20000 个查询，候选值共 5000 个。
2. 分别用列表和集合统计命中数量。
3. 每种实现重复运行 3 次，计算中位数与加速比。
4. 验证两种实现结果完全一致。

## 结果

列表与集合的命中数相同；集合查询显著更快，证明数据结构选择会直接影响程序性能。
